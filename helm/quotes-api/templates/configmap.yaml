apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "quotes-api.fullname" . }}-config
  labels:
    {{- include "quotes-api.labels" . | nindent 4 }}
data:
  application.properties: |
    server.port={{ .Values.app.port }}
    spring.application.name={{ .Values.app.name }}
    greeting.message=Hello from Spring Boot!