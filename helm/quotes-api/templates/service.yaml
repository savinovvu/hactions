apiVersion: v1
kind: Service
metadata:
  name: {{ include "quotes-api.fullname" . }}
  labels:
    {{- include "quotes-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "quotes-api.selectorLabels" . | nindent 4 }}
