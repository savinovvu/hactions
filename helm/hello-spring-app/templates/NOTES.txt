Приложение {{ .Chart.Name }} установлено в неймспейс {{ .Release.Namespace }} как {{ include "hello-spring-app.fullname" . }}.
  ⚡️ Версия чарта: {{ .Chart.Version }}
  ⚡️ Версия образа: {{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}
  ⚡️ Версия приложения: {{ .Chart.AppVersion }}

Получи доступ к API по адресу:
{{- if .Values.ingress.enabled }}
  🌏 http://{{ .Values.ingress.host }}
{{- else }}
  ⚡️ kubectl port-forward service/{{ include "hello-spring-app.fullname" . }} http
{{- end }}
